[project]
name = "diarium-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Valdir Junior", email = "valdir.junior.dev@gmail.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
diarium-app = "diarium_app:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
